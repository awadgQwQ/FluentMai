# FluentMai 抓包环境配置指南 (Windows)

## 1. 启动抓包代理

在 FluentMai 项目根目录打开终端，执行：

```powershell
mitmdump -s interceptor.py -p 8080
```

参数说明：
- `-s interceptor.py` — 加载拦截插件
- `-p 8080` — 监听 8080 端口

首次运行后，mitmproxy 会自动在 `%USERPROFILE%\.mitmproxy\` 目录下生成根证书。

---

## 2. 安装 mitmproxy 根证书

证书文件位于：

```
%USERPROFILE%\.mitmproxy\mitmproxy-ca-cert.p12
```

### 安装步骤

1. 按 `Win + R`，粘贴上述路径，回车。
2. 双击 `mitmproxy-ca-cert.p12` 文件。
3. 选择 **"本地计算机"** → 下一步 → 下一步（密码留空）。
4. 选择 **"将所有证书都放入下列存储"** → 点击 **"浏览"**。
5. 选择 **"受信任的根证书颁发机构"** → 确定 → 下一步 → 完成。
6. 安全警告弹窗中点击 **"是"**。

> **注意**：证书密码默认为空，导入时直接留空即可。如果 mitmproxy 版本不同导致证书有密码，请查阅 mitmproxy 官方文档。

---

## 3. 配置 Windows 系统代理

1. 打开 **设置** → **网络和 Internet** → **代理**。
2. 在 **"手动设置代理"** 区域，点击 **"使用代理服务器"** 旁的 **"设置"**。
3. 开启 **"使用代理服务器"** 开关。
4. 代理 IP 地址填入：`127.0.0.1`
5. 端口填入：`8080`
6. 点击 **"保存"**。

> **重要**：抓包结束后，记得回到此处**关闭代理开关**，否则关闭 mitmdump 后浏览器将无法上网。

---

## 4. 验证

保持终端中 `mitmdump` 运行，在手机或模拟器上打开舞萌 DX 成绩页面。

如果拦截成功，终端会打印：

```
✅ 成功捕获舞萌成绩页面！
   → 已保存至 html_dumps/record_diff_3_1710000000.html  (45.2 KB)
```

HTML 文件保存在项目根目录的 `html_dumps/` 文件夹中。
